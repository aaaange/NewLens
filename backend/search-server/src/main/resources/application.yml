server:
  port: 8080

spring:
  kafka:
    admin:
      auto-create: true

elasticsearch:
  host: ${ES_HOST}
  port: ${ES_PORT}
  scheme: ${ES_SCHEME}

redis:
  host: ${REDIS_HOST}
  port: ${REDIS_PORT}

