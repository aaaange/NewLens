server:
  port: 9002

spring:
  kafka:
    admin:
      auto-create: true

elasticsearch:
  host: ${ES_HOST}
  port: ${ES_PORT}
  scheme: ${ES_SCHEME}
  username: ${ES_USERNAME}
  password: ${ES_PASSWORD}

redis:
  host: ${REDIS_HOST}
  port: ${REDIS_PORT}
  password: ${REDIS_PASSWORD}
